{
  "name": "sequelize-migrate",
  "version": "1.0.0",
  "description": "sequelize migration nodejs postgresql",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate:up": "npx sequelize-cli db:migrate",
    "migrate:undo": "npx sequelize-cli db:migrate:undo",
    "db:seed": "npx sequelize-cli db:seed:all",
    "seed:generate": "npx sequelize-cli seed:generate --name demo-user",
    "db:migrate": "npx sequelize-cli db:migrate",
    "model:generate": "npx sequelize-cli model:generate --name Users --attributes firstName:string,lastName:string,email:string,address1:string,address2:string,city:string,state:string,phone:string,password:string",
    "db:create": "npx sequelize-cli db:create",
    "start": "nodemon server"
  },
  "keywords": [
    "sequelize",
    "migration",
    "nodejs",
    "postgresql"
  ],
  "author": "Jason",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.20.1",
    "config": "^3.3.9",
    "cookie-parser": "^1.4.6",
    "debug": "^4.3.4",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "nodemon": "^2.0.20",
    "path": "^0.12.7",
    "pg": "^8.9.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.28.0",
    "sequelize-cli": "^6.6.0"
  }
}
